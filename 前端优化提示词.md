# 前端优化提示词

## 项目概述

这是一个**在线伴学平台**，技术栈为 Vue 3 + Vite + Pinia + Vue Router + Axios，包含以下核心功能：

- 用户认证（登录/注册）
- 自习室创建、加入、列表浏览
- 实时视频（WebRTC + WebSocket）
- AI离席检测
- 隐私模式（模糊/手部遮挡）
- 学习时长排行榜
- 个人中心与标签管理

---

## 优化方向与提示词

### 1. 整体UI/UX优化

```
请参考以下设计原则优化本项目前端：

1. **视觉风格**：采用现代卡片式设计，使用柔和的渐变色和阴影效果
   - 主色调：#2d6a4f（深绿色），辅助色：#40916c
   - 背景色：渐变背景如 linear-gradient(135deg, #667eea 0%, #764ba2 100%)
   - 卡片：圆角16px-20px，轻阴影

2. **动画效果**：
   - 页面切换使用淡入淡出动画（transition: opacity 0.3s ease）
   - 按钮悬停添加微上移效果（transform: translateY(-2px)）
   - 加载状态使用骨架屏（skeleton loading）
   - 列表项添加交错入场动画

3. **响应式设计**：
   - 移动端使用底部导航栏
   - 卡片在移动端全宽显示
   - 视频网格在小屏幕下自适应

4. **空状态设计**：
   - 无数据时显示友好的插画和引导文案
   - 错误状态显示重试按钮

5. **图标与插图**：
   - 使用 Lucide Icons 或 Heroicons 图标库
   - 添加与学习场景相关的SVG插图
```

---

### 2. Home.vue 首页优化

```
请优化 Home.vue 首页，参考以下方案：

1. **欢迎区域**：
   - 添加动态问候语（根据时间段：早上好/下午好/晚上好）
   - 显示用户头像和昵称，使用圆形头像设计
   - 添加今日学习目标进度条

2. **数据卡片**：
   - 今日学习时长卡片（带图标和动画）
   - 累计学习天数
   - 击败百分比徽章
   - 使用数字滚动动画（countUp效果）

3. **快捷入口**：
   - 改造成3列或4列网格布局
   - 每个入口添加图标（📚自习室 🏆排行榜 👤个人中心 ⚙️设置）
   - 悬停时显示简短说明

4. **推荐自习室**：
   - 显示当前在线人数最多的房间
   - 一键快速加入按钮
   - 房间标签展示（考研/期末/考公等）

5. **学习小贴士**：
   - 每日一句励志语录（可调用外部API如一言API：https://hitokoto.cn/）
   - 学习技巧小卡片

6. **布局改进**：
   - 使用 CSS Grid 实现响应式布局
   - 添加 Section 区块划分
   - 顶部添加天气/日期组件
```

---

### 3. StudyRoomDetail.vue 房间详情优化

```
请优化 StudyRoomDetail.vue 房间详情页：

1. **房间信息卡片**：
   - 使用玻璃态设计（backdrop-filter: blur）
   - 主题突出显示（大字号+背景色）
   - 房间状态实时更新指示器

2. **视频区域**：
   - 添加视频边框灯带效果（根据房间主题变色）
   - 远端用户视频添加昵称标签浮层
   - 断线重连状态显示
   - 本地视频添加静音/关摄像头状态图标
   - 视频加载时显示优雅的占位动画

3. **隐私模式UI**：
   - 隐私模式切换使用切换按钮样式
   - 实时预览模糊效果
   - AI检测状态指示器（检测中/正常/离席警告）

4. **操作按钮**：
   - 底部固定操作栏（离开房间/开关摄像头/开关麦克风）
   - 使用圆形图标按钮+Tooltip提示
   - 房间销毁按钮添加二次确认动画

5. **房间成员展示**：
   - 头像+昵称列表
   - 在线状态绿点指示
   - 当前发言用户高亮效果
   - 成员离开/加入的Toast通知

6. **学习数据展示**：
   - 实时计时器动画（数字跳动效果）
   - 学习时长占比环形图
   - 专注度评分（如果接入AI分析）

7. **添加背景音乐功能**：
   - 白噪音/轻音乐选择
   - 音量控制滑块
```

---

### 4. Rank.vue 排行榜优化

```
请优化 Rank.vue 排行榜页面：

1. **个人排名突出显示**：
   - 当前用户排名置顶显示（大卡片）
   - 背景使用渐变色突出
   - 排名徽章动画效果

2. **排行榜列表**：
   - 前三名使用特殊样式（金/银/铜牌图标）
   - 排名数字使用大字号
   - 学习时长使用进度条可视化
   - 用户头像显示

3. **时间筛选**：
   - 日/周/月切换Tab
   - 日期选择器优化

4. **数据可视化**：
   - 添加简单柱状图展示前10名时长对比
   - 使用CSS绘制简易图表
   - 或接入ECharts轻量级图表

5. **激励元素**：
   - 距离上一名还差多少时间的提示
   - 连续学习天数徽章
   - 进步幅度箭头指示

6. **空状态**：
   - 无数据时显示排名上升示意图
   - 引导文案："暂无记录，快去学习吧！"
```

---

### 5. Personal.vue 个人中心优化

```
请优化 Personal.vue 个人中心页面：

1. **头部信息区**：
   - 背景使用渐变色或SVG图案
   - 头像添加VIP/等级徽章
   - 编辑资料按钮

2. **学习数据统计**：
   - 本周学习时长柱状图
   - 学习日历热力图（GitHub贡献图风格）
   - 最长连续学习记录

3. **成就系统**：
   - 已获得徽章展示（学习新星、坚持7天等）
   - 进度解锁展示
   - 接入外部徽章API如Open Badges

4. **设置功能**：
   - 头像上传（可调用外部图片上传API）
   - 通知设置
   - 隐私设置
   - 账号安全（修改密码等）

5. **学习报告**：
   - 周报生成按钮
   - 数据导出功能
   - 分享到社交平台
```

---

### 6. Login.vue / Register.vue 优化

```
请优化登录/注册页面：

1. **登录页**：
   - 添加背景装饰动画（浮动书本/铅笔等SVG）
   - 使用更大的标题和输入框
   - 密码找回链接
   - 第三方登录按钮占位（后续可接入微信等）
   - 表单验证动画（输入框边框变色）

2. **注册页**：
   - 步骤指示器（1.输入信息 2.设置标签 3.完成）
   - 手机号实时格式校验
   - 密码强度指示器（弱/中/强）
   - 注册协议勾选框

3. **通用优化**：
   - 页面切换使用滑动动画
   - 加载状态按钮显示loading动画
   - 错误提示使用shake动画
   - 成功跳转变打勾动画
```

---

### 7. 通用组件优化

```
请优化以下通用组件：

1. **CommonToast.vue**：
   - 支持多种类型（success/error/warning/info）
   - 添加图标
   - 支持进度条显示（auto dismiss倒计时）
   - 支持手动关闭按钮

2. **StudyRoomList.vue**：
   - 房间卡片添加动画效果
   - 空状态添加插图
   - 加载状态骨架屏
   - 添加房间搜索功能

3. **PrivacyMode.vue**：
   - 优化隐私模式切换UI
   - 添加实时预览效果
```

---

### 8. 外部资源利用建议

```
建议接入以下外部API和模板资源优化前端：

1. **图标库**：
   - Lucide Icons（https://lucide.dev/）
   - Heroicons（https://heroicons.com/）

2. **动画库**：
   - VueUse（https://vueuse.org/）提供useTransition等
   - GSAP（https://greensock.com/）用于复杂动画
   - CSS动画优先，减少依赖

3. **UI模板参考**：
   - Tailwind CSS 组件库（https://tailwindcss.com/）
   - Naive UI（https://www.naiveui.com/）
   - Element Plus（https://element-plus.org/）

4. **外部API**：
   - 一言API（https://hitokoto.cn/）获取励志语录
   - 天气API（和风天气等）用于首页展示
   - Unsplash API 获取学习场景图片

5. **数据可视化**：
   - ECharts（https://echarts.apache.org/）轻量级图表
   - Chart.js（https://www.chartjs.org/）
   - 纯CSS实现简单图表

6. **字体**：
   - 使用Google Fonts的中文字体
   - 思源黑体 / 阿里巴巴普惠体
```

---

### 9. 性能优化建议

```
请进行以下性能优化：

1. **图片优化**：
   - 组件图片懒加载（使用 IntersectionObserver）
   - 头像使用WebP格式（提供fallback）
   - 图标使用SVG内联而非图片

2. **代码分割**：
   - 路由懒加载：() => import('./views/xxx.vue')
   - 组件按需引入

3. **动画优化**：
   - 使用 transform 和 opacity 实现动画
   - 避免 layout thrashing
   - 使用 will-change 提示浏览器

4. **用户体验优化**：
   - 添加页面加载骨架屏
   - 按钮防抖处理
   - 表单节流验证
   - 错误边界处理

5. **状态管理优化**：
   - 合理使用 Pinia store
   - 避免不必要的响应式
```

---

## 具体代码实现示例

### 示例1：首页添加动态问候语

```javascript
const greeting = computed(() => {
  const hour = new Date().getHours()
  if (hour < 6) return '夜深了，注意休息'
  if (hour < 9) return '早上好，开始学习吧'
  if (hour < 12) return '上午好，保持专注'
  if (hour < 14) return '中午好，适当休息'
  if (hour < 18) return '下午好，继续加油'
  if (hour < 22) return '晚上好，充实的一天'
  return '夜深了，早点休息'
})
```

### 示例2：数字滚动动画

```javascript
function animateCount(target, duration = 1000) {
  const start = 0
  const startTime = performance.now()
  function update(currentTime) {
    const elapsed = currentTime - startTime
    const progress = Math.min(elapsed / duration, 1)
    const value = Math.floor(progress * target)
    // 更新DOM
    if (progress < 1) requestAnimationFrame(update)
  }
  requestAnimationFrame(update)
}
```

### 示例3：骨架屏组件

```vue
<template>
  <div class="skeleton" :style="{ width, height, borderRadius }"></div>
</template>

<style scoped>
.skeleton {
  background: linear-gradient(90deg, #f0f0f0 25%, #e0e0e0 50%, #f0f0f0 75%);
  background-size: 200% 100%;
  animation: shimmer 1.5s infinite;
}
@keyframes shimmer {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}
</style>
```

### 示例4：路由懒加载

```javascript
// router/index.js
const routes = [
  {
    path: '/home',
    component: () => import('./views/Home.vue')
  },
  {
    path: '/study-room',
    component: () => import('./views/StudyRoom.vue')
  }
]
```

---

## 文件结构参考

```
frontend/src/
├── views/
│   ├── Home.vue           # 首页 - 需优化
│   ├── Login.vue           # 登录页 - 需优化
│   ├── Register.vue        # 注册页 - 需优化
│   ├── Personal.vue        # 个人中心 - 需优化
│   ├── Rank.vue            # 排行榜 - 需优化
│   ├── StudyRoom.vue       # 自习室列表
│   ├── StudyRoomDetail.vue # 房间详情 - 需优化
│   └── Tags.vue            # 标签选择
├── components/
│   ├── CommonToast.vue      # Toast组件 - 需优化
│   ├── PrivacyMode.vue     # 隐私模式 - 需优化
│   └── StudyRoomList.vue   # 房间列表 - 需优化
├── composables/
│   └── useAiDetection.js   # AI检测
├── api/
│   ├── client.js
│   ├── user.js
│   ├── studyRoom.js
│   └── duration.js
├── store/
│   └── index.js            # Pinia状态管理
└── assets/
    └── base.css            # 全局样式
```

---

## 实施建议

1. **分模块进行**：每次优化1-2个视图，便于代码审查和调试
2. **保持风格一致**：定义全局CSS变量，统一颜色、字体、间距
3. **渐进式增强**：先保证功能完整，再添加动画效果
4. **移动优先**：先确保移动端体验，再优化桌面端
5. **性能监控**：优化后使用 Chrome DevTools 检查性能指标
